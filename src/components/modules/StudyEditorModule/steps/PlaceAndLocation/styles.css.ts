import { style } from '@vanilla-extract/css';
import { default as rootStyles } from '../../styles.css';

const textArea = style({
	minHeight: 58,
});

const styles = {
	textArea,
};

export default styles;
