import { style } from '@vanilla-extract/css';

const launchKakao = style({
	fontSize: 14,
	textDecoration: 'underline',
	cursor: 'pointer',
	marginTop: 10,
});

const styles = {
	launchKakao,
};

export default styles;
